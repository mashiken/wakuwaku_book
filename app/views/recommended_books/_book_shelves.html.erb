
<table class=' table table-hover table-inverse order_table_text'>
	<thead>
		<tr>
			<th>MY本棚 書籍画像</th>
			<th>書籍タイトル / カナ</th>
			<th>著者名 / カナ</th>
			<th>出版社名</th>
			<th>発売日</th>
			<th>書籍確定</th>
		    <th>書籍詳細</th>
		<tr>
	</thead>

  <% book_shelves.each do |book_shelf| %>
	    <% books = RakutenWebService::Books::Book.search(isbn: book_shelf.book_id.to_i) %>
	    <% books.each do |item| %>
			<tbody>
				<tr>
					<td><%= image_tag(item.medium_image_url) %></td>
					<td><%= item.title %><br><%= item.title_kana %></td>
					<td><%= item.author %><br><%= item.author_kana %></td>
					<td><%= item.publisher_name %></td>
					<td><%= item.sales_date %></td>
					<td><%= link_to "書籍決定",  finish_recommended_book_path(id: recommended_user.id, book_id: book_shelf.book_id), class: "btn btn-md btn-success", style: "color:white;" %></td>
					<td><%= link_to "詳細情報",  book_path(item.isbn), class: "btn btn-md btn-info", style: "color:white;" %></td>
				<tr>
			</tbody>
	    <% end %>
	<% end %>
</table>

