<p>★商品画像やタイトルをクリックすると、書籍の詳細ページに移動します。</p>
<p>★ユーザー画像をクリックすると、そのユーザーのページに移動します。</p>

<div class="table-responsive">
	<table class=' table table-hover table-inverse order_table_text'>
		<thead>
			<tr>
				<th>書籍をオススメしたユーザー</th>
				<th>書籍画像</th>
				<th>書籍タイトル / カナ</th>
				<th>著者名 / カナ</th>
				<th>出版社名</th>
				<th>オススメ文</th>
			<tr>
		</thead>

	    <% @recommended_book.each do |recommended_book| %>
			<% books = RakutenWebService::Books::Book.search(isbn: recommended_book.book_id.to_i) %>
			<% books.each do |item| %>
				<tbody>
					<tr>
						<td>
							<%= link_to user_path(recommended_book.user.id) do %>
								<%= attachment_image_tag recommended_book.user, :image, size: "100x100", format: "jpg", fallback:"no_image.jpg" %>
								<br>
					            <%= recommended_book.user.nickname %>
				            <% end %>
						</td>
		                <td><%= link_to image_tag(item.medium_image_url), book_path(item.isbn) %></td>
				        <td><%= link_to item.title, book_path(item.isbn) %><br><%= link_to item.title_kana,book_path(item.isbn) %></td>
						<td><%= item.author %><br><%= item.author_kana %></td>
						<td><%= item.publisher_name %></td>
						<td>
							タイトル : <%= recommended_book.title %><br>
							本文 : <%= recommended_book.text %>
						</td>
	                </tr>
				</tbody>
		    <% end %>
		<% end %>
		<%= paginate @recommended_book, class: "paginate" %>
	</table>
</div>
